# 엔티언펜싱클럽 홈페이지 관리자 사용설명서

---

## 1. 처음 한 번만 하는 준비

### 1) Supabase 프로젝트 만들기

1. <https://supabase.com> 에서 **클럽 이메일로** 회원가입하고 새 프로젝트를 만듭니다.
   - Region 은 `Northeast Asia (Seoul)` 을 고르세요.
   - 프로젝트 비밀번호는 따로 적어 두세요.
2. 왼쪽 메뉴 **SQL Editor** 를 열고, `supabase/schema.sql` 파일 내용을 통째로 붙여넣고 실행합니다.
3. 같은 자리에서 `supabase/seed.sql` 도 붙여넣고 실행합니다.
   → 지점 2곳, 프로그램 4개, 지도자 3명이 들어갑니다.

### 2) 관리자 계정 만들기

Supabase 왼쪽 메뉴 **Authentication → Users → Add user** 에서
사장님이 쓰실 **이메일과 비밀번호**를 직접 등록합니다.
(홈페이지에는 회원가입 기능이 없습니다. 이 계정 하나로만 로그인합니다.)

### 3) 홈페이지에 연결 정보 넣기

Supabase **Project Settings → API** 에서 두 값을 복사합니다.

| 항목 | 어디에 넣나 |
|---|---|
| Project URL | `assets/js/config.js` 의 `SUPABASE_URL` |
| anon public 키 | `assets/js/config.js` 의 `SUPABASE_ANON_KEY` |

```js
window.ANTAEAN_CONFIG = {
  SUPABASE_URL: "https://xxxxx.supabase.co",
  SUPABASE_ANON_KEY: "eyJhbGci....",
  KAKAO_URL: "https://pf.kakao.com/_xxxxx"
};
```

> **`service_role` 키는 절대 넣지 마세요.** 반드시 `anon` 키만 씁니다.
> anon 키는 공개되어도 안전합니다. 실제 권한은 Supabase 쪽 보안 규칙(RLS)이 막습니다.

---

## 2. 로그인

주소창에 **`홈페이지주소/admin/login.html`** 을 입력하고,
위에서 만든 이메일·비밀번호로 로그인합니다.

로그인하면 왼쪽에 메뉴 8개가 보입니다.

---

## 3. 화면별 사용법

### 대시보드
- 아직 확인하지 않은 문의가 몇 건인지 맨 먼저 보여 줍니다.
- 최근 문의 5건에서 이름을 누르면 문의함으로 바로 갑니다.

### 문의함 ★ 가장 자주 쓰는 화면
홈페이지에서 **체험수업 / 상담신청**을 하면 여기로 들어옵니다.

- 노란 **신규** 표시가 아직 연락하지 않은 문의입니다.
- **열기** 를 누르면 문의내용 전체와 메모칸이 나옵니다.
- 연락처를 누르면 휴대폰에서 바로 전화가 걸립니다.
- 연락을 마쳤으면 **연락완료로 바꾸기** 를 눌러 두세요. 그래야 새 문의와 구분됩니다.
- **메모** 는 클럽 내부용입니다. 홈페이지에는 보이지 않습니다.

### 공지사항
- 제목만 넣어도 등록됩니다.
- **상단 고정** 을 켜면 목록 맨 위에 붙습니다.
- 사진은 한 장까지. 올리면 자동으로 가로 1600px로 줄여 저장합니다.
- 등록한 공지는 홈페이지 메인의 공지 영역에 최신 3건이 표시됩니다.

### 프로그램
- 취미반 / 꿈나무반 / 선수반 / 성인반 4개의 **대상·수업 특징·수업시간** 을 고칩니다.
- 반을 새로 만들거나 지우지는 않습니다. 내용만 바꾸는 화면입니다.
- 한 줄에 하나씩 적으면 홈페이지에서도 줄이 나뉩니다.

### 시간표
- 위에서 **송도점 / 배곧점** 을 먼저 고릅니다.
- **줄 추가** 를 눌러 요일·시간·반·비고를 입력합니다.
- 다 입력했으면 반드시 아래 **전체 저장** 을 누르세요.
- 숫자(순서)가 작을수록 위에 나옵니다.
- 시간표를 하나도 넣지 않으면 홈페이지에는 "등록된 시간표가 없습니다." 라고 나옵니다.

### 지도자
- **지도자 추가** 를 누르면 빈 칸이 하나 생깁니다. 이름부터 바꿔 주세요.
- 학력·선수경력·자격증·지도경력·수상내역은 **한 줄에 하나씩** 적습니다.
- 사진을 올리면 강사진 페이지에 표시됩니다. 사진이 없으면 이름 이니셜이 대신 나옵니다.
- 항목을 고친 뒤에는 그 사람 칸의 **저장** 을 눌러야 반영됩니다.

### 갤러리
- 지점을 고르고 사진을 올립니다. 여러 장을 한 번에 고를 수 있습니다.
- 학원소개 페이지의 시설 사진으로 표시됩니다.
- 사진 아래 숫자를 바꾸면 순서가 바뀝니다(작을수록 앞).

### 클럽 일상
- 커뮤니티 페이지의 "클럽 일상" 사진입니다. 여러 장을 한 번에 올릴 수 있습니다.
- 사진을 한 장이라도 올리면 홈페이지에 들어 있던 기본 사진 대신 올린 사진만 표시됩니다.
- 사진 아래 숫자를 바꾸면 순서가 바뀝니다(작을수록 앞).

### 대회 기록
- 커뮤니티 페이지의 "대회 기록"에 표시됩니다.
- 제목에 대회 이름, 내용에 참가자와 성적을 적으세요. 줄바꿈은 그대로 나옵니다.
- 표시 순서 숫자가 작을수록 위에 나옵니다.

### 지점 정보
- 오시는 길과 홈페이지 맨 아래에 나오는 주소·전화번호·주차·인스타그램을 고칩니다.
- **카카오톡 채널 주소** 를 여기에 넣으면 홈페이지의 "카카오톡 문의" 버튼이 연결됩니다.

---

## 4. 알아 두실 점

- **삭제는 되돌릴 수 없습니다.** 삭제를 누르면 항상 한 번 더 물어봅니다.
- 사진은 올릴 때 자동으로 크기를 줄이므로, 휴대폰 사진을 그대로 올리셔도 됩니다.
- 관리자 화면은 검색엔진에 노출되지 않습니다.
- 홈페이지를 열어 둔 채 관리자에서 내용을 바꿨다면, 홈페이지를 새로고침해야 보입니다.

### 무료 플랜 주의
Supabase 무료 프로젝트는 **7일 동안 아무 접속이 없으면 일시정지**됩니다.
홈페이지에 방문자가 있으면 자동으로 유지되지만, 확실히 하려면
하루 한 번 아래 주소를 호출하는 예약 작업을 걸어 두면 됩니다.

```
{SUPABASE_URL}/rest/v1/branches?select=id&limit=1
```

---

## 5. 문제가 생기면

| 증상 | 확인할 것 |
|---|---|
| 로그인 화면에 "config.js 에 …" 문구가 뜬다 | `assets/js/config.js` 에 URL·키를 넣었는지 |
| 로그인이 안 된다 | Supabase → Authentication → Users 에 계정이 있는지 |
| 저장은 되는데 홈페이지에 안 보인다 | 홈페이지를 새로고침(Ctrl+F5) |
| 프로그램·지점 화면이 비어 있다 | `supabase/seed.sql` 을 실행했는지 |
| 사진이 안 올라간다 | schema.sql 이 정상 실행되어 `public-images` 버킷이 만들어졌는지 |
